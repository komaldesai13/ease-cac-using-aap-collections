# AAP Platform State Report

| | |
|---|---|
| **Gateway** | `{{ aap_gateway.hostname }}` |
| **Generated** | {{ ansible_date_time.iso8601 }} |
| **Generated by** | `ansible.platform.gateway_api` lookup |
| **Scope** | resources prefixed `{{ demo_prefix }}` |

---

## Platform totals

| Resource | Total on this Gateway | In scope for this report |
|---|---:|---:|
| Organizations | {{ live_organizations | length }} | {{ demo_orgs_live | length }} |
| Teams | {{ live_teams | length }} | {{ demo_teams_live | length }} |
| Users | {{ live_users | length }} | {{ demo_users_live | length }} |
| Role definitions | {{ live_roles | length }} | {{ demo_roles_live | length }} |

## Privileged accounts

Superusers hold unrestricted access to the whole platform. This list should be
short, and every entry should be explainable.

| Username | Email | Last login |
|---|---|---|
{% for u in live_superusers %}
| `{{ u.username }}` | {{ u.email | default('-') }} | {{ u.last_login | default('never') }} |
{% endfor %}

**Count: {{ live_superusers | length }}**

---

## Organizations in scope

{% if demo_orgs_live | length == 0 %}
_None. Run `playbooks/01-tenant-onboarding.yml` first._
{% else %}
| Name | Description | ID |
|---|---|---:|
{% for o in demo_orgs_live %}
| {{ o.name }} | {{ o.description | default('-') }} | {{ o.id }} |
{% endfor %}
{% endif %}

## Teams in scope

{% if demo_teams_live | length == 0 %}
_None._
{% else %}
| Name | Description | ID |
|---|---|---:|
{% for t in demo_teams_live %}
| {{ t.name }} | {{ t.description | default('-') }} | {{ t.id }} |
{% endfor %}
{% endif %}

## Users in scope

{% if demo_users_live | length == 0 %}
_None._
{% else %}
| Username | Name | Email | Platform auditor | Superuser |
|---|---|---|---|---|
{% for u in demo_users_live %}
| `{{ u.username }}` | {{ u.first_name | default('') }} {{ u.last_name | default('') }} | {{ u.email | default('-') }} | {{ 'yes' if u.is_platform_auditor | default(false) else 'no' }} | {{ 'yes' if u.is_superuser | default(false) else 'no' }} |
{% endfor %}
{% endif %}

## Custom roles in scope

{% if demo_roles_live | length == 0 %}
_None. Run `playbooks/03-rbac-as-code.yml` first._
{% else %}
| Role | Applies to | Permissions |
|---|---|---|
{% for r in demo_roles_live %}
| {{ r.name }} | `{{ r.content_type | default('-') }}` | {{ r.permissions | default([]) | join(', ') }} |
{% endfor %}
{% endif %}

---

_Generated from live Gateway state. The configuration intended to produce this
state lives in `vars/` in the same repository — if the two disagree, that is
drift, and `playbooks/02-idempotency-and-drift.yml` will find it._
